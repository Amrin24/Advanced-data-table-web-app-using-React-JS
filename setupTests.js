// Import the custom jest matchers from jest-dom
import '@testing-library/jest-dom';

// Optional: Setup for additional testing utilities or global configurations
// You can include any global mocks or setup files here if needed
// Example: Mocking a global function if needed
// global.someFunction = jest.fn();

// You might also want to set up some global configurations or imports here
// For example, if you are using certain libraries or need specific settings for all tests
